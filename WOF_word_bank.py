import random

word_bank = {'Easy'[('tiger'), ('clover'), ('fork'), ('cloud')],
    'Medium'[('rasberry'), ('automate'), ('fortune'), ('javascript')],
    'Hard'[('associate'), ('plebotomist'), ('lion king'), ('fast and furious')]
}

# def computer_choice(word_dict):
#     for difficulty, word_bank in word_dict.items():
#         for word in word_bank:
#             print(f"You chose {difficulty}, your word is {word_bank}")

    #choice = print(len(random.choice(word_bank)))


computer_choice()